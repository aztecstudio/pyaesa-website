import { Branches, Contact, MainProducts, Overview } from '@/components';

export default function Home() {
	return (
		<>
			<Overview />
			<MainProducts />
			<Branches />
			<Contact />
		</>
	);
}
